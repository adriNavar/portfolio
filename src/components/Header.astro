<header class="fixed top-4 left-0 w-full z-[100] px-4">
    <div class="max-w-4xl mx-auto flex items-center justify-center">
        
        <div class="hidden md:flex w-full bg-[#050505]/90 backdrop-blur-xl border border-white/20 rounded-full py-3 px-8 items-center justify-between shadow-2xl">
            <a href="#inicio" class="flex items-center gap-3 group shrink-0">
                <div class="w-8 h-8 rounded-full bg-emerald-500 flex items-center justify-center font-black text-[#050505] text-xs transition-transform group-hover:scale-110 shadow-[0_0_15px_rgba(16,185,129,0.4)]">
                    A
                </div>
                <span class="font-bold text-sm text-white tracking-tight group-hover:text-emerald-400 transition-colors">Adrian.dev</span>
            </a>

            <nav class="flex items-center gap-x-8 text-[11px] uppercase tracking-[0.25em] font-mono">
                <a href="#inicio" class="nav-link text-white hover:text-emerald-400 transition-all py-1">Inicio</a>
                <a href="#consola" data-tab="exp" class="nav-link text-white hover:text-emerald-400 transition-all py-1">Experiencia</a>
                <a href="#consola" data-tab="proy" class="nav-link text-white hover:text-emerald-400 transition-all py-1">Proyectos</a>
                <a href="#consola" data-tab="about" class="nav-link text-white hover:text-emerald-400 transition-all py-1">Sobre mí</a>
                <a href="#consola" data-tab="contact" class="nav-link text-white hover:text-emerald-400 transition-all py-1">Contacto</a>
            </nav>
            <div class="w-10"></div>
        </div>

        <nav class="md:hidden w-full bg-[#050505]/95 backdrop-blur-2xl border border-emerald-500/30 rounded-2xl p-1.5 flex justify-around items-center shadow-[0_10px_30px_rgba(0,0,0,0.5)]">
            <a href="#inicio" class="nav-link flex flex-col items-center gap-1 p-2 transition-all">
                <div class="size-1 rounded-full bg-emerald-400 opacity-0 dot-active transition-all"></div>
                <span class="text-[9px] font-black uppercase tracking-tighter text-white/100">Inicio</span>
            </a>
            <a href="#consola" data-tab="exp" class="nav-link flex flex-col items-center gap-1 p-2 transition-all">
                <div class="size-1 rounded-full bg-emerald-400 opacity-0 dot-active transition-all"></div>
                <span class="text-[9px] font-black uppercase tracking-tighter text-white/100">Experiencia</span>
            </a>
            <a href="#consola" data-tab="proy" class="nav-link flex flex-col items-center gap-1 p-2 transition-all">
                <div class="size-1 rounded-full bg-emerald-400 opacity-0 dot-active transition-all"></div>
                <span class="text-[9px] font-black uppercase tracking-tighter text-white/100">Proyectos</span>
            </a>
            <a href="#consola" data-tab="about" class="nav-link flex flex-col items-center gap-1 p-2 transition-all">
                <div class="size-1 rounded-full bg-emerald-400 opacity-0 dot-active transition-all"></div>
                <span class="text-[9px] font-black uppercase tracking-tighter text-white/100">Perfil</span>
            </a>
            <a href="#consola" data-tab="contact" class="nav-link flex flex-col items-center gap-1 p-2 transition-all">
                <div class="size-1 rounded-full bg-emerald-400 opacity-0 dot-active transition-all"></div>
                <span class="text-[9px] font-black uppercase tracking-tighter text-white/100">Contacto</span>
            </a>
        </nav>
    </div>
</header>

<style>
    /* Estilos comunes para links activos */
    .nav-link.active {
        color: #34d399 !important;
        font-weight: 900;
        text-shadow: 0 0 10px rgba(52, 211, 153, 0.5);
    }

    /* Punto indicador en PC */
    .hidden.md\:flex .nav-link.active {
        position: relative;
    }
    .hidden.md\:flex .nav-link.active::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 50%;
        transform: translateX(-50%);
        width: 4px;
        height: 4px;
        background-color: #34d399;
        border-radius: 50%;
        box-shadow: 0 0 8px #34d399;
    }

    /* Punto indicador en Mobile */
    .nav-link.active .dot-active {
        opacity: 1;
        box-shadow: 0 0 10px #34d399;
        transform: scale(1.2);
    }

    /* Blanco fuerte para PC */
    .hidden.md\:flex .nav-link {
        font-weight: 700;
        color: white;
    }
</style>